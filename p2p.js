const {
    getNewestBlock,
    isBlockStructureValid,
    replaceChain,
    getBlockchain,
    addBlockToChain,
} = Blockchain;

const Blockchain = require("./blockchain");

const WebSockets = require("ws");
